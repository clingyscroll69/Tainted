"""The Structure register — deterministic static analysis.

Reads code and recovers facts: routes, queries, RLS policies, tool graphs. Fast, exhaustive,
incapable of inventing what isn't there, and blind to meaning (that is the LLM's job).
"""
